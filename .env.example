# Environment Variables for GitHub Actions
# Add these as secrets in your GitHub repository settings

# Application environment variables
API_SECRET=your_api_secret
TOKEN_HOUR_LIFESPAN=1
CONNECTION_STRING=root:password@tcp(mysql_host:3306)/database_name?charset=utf8mb4&parseTime=True&loc=Local

# Note: MySQL should be running in a separate container/service
# The CONNECTION_STRING should point to your existing MySQL instance